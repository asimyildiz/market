/**
 * interface to represent group radio model
 * @interface GroupRadioModel
 */
export interface GroupRadioModel {
  /**
   * key to use while selecting an element
   * @type {string}
   */
  key: string;

  /**
   * value to use while displaying an element
   * @type {string}
   */
  value: string;
}
